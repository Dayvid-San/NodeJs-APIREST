const { uuid } = require('uuidv4');

export class User {
    
    constructor(props){
        //Object.assign(this, props)
        const id
        const name
        const email
        const password

        if(!id) {
            this.id = uuid()
        }
    }
}